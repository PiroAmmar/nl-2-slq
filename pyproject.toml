[project]
name = "nlp2sql"
version = "0.1.0"
description = "NL-to-SQL RAG pipeline"
authors = [
    { name = "PiroAmmar", email = "piroammar388@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "streamlit",
    "google-genai",
    "groq",
    "chromadb",
    "plotly",
    "pandas",
    "openpyxl",
    "python-dotenv",
    "tenacity",
]
